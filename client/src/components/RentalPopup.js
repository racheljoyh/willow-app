import { useState, useEffect } from "react";

function RentalPopup() {
  // useEffect(() => {
  //     fetch("/listings/").then((r) => {
  //       if (r.ok) {
  //         r.json().then((rentals) => {
  //           setMyRentals(rentals);
  //         });
  //       }
  //     });
  // }, []);

  return <></>;
}

export default RentalPopup;
